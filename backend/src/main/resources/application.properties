spring.application.name=e-com

# PostgreSQL database configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/core_ecom
spring.datasource.username=postgres
spring.datasource.password=Tuong2005@

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Run data.sql to fix existing records after schema update
spring.sql.init.mode=always

# JWT Configuration
spring.app.jwtSecret=mySecretKey123912738aopsgjnspkmndfsopkvajoirjg94gf2opfng2moknm
spring.app.jwtExpirationMs=300000000
spring.ecom.app.jwtCookieName=springBootEcom

# Logging
logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=INFO
logging.level.org.springframework.security=INFO
logging.level.com.example.ecom=INFO

# Images
project.image=images/
image.base.url=http://localhost:8080/images

# Frontend CORS
frontend.url=http://localhost:5173/

# Stripe Payment (use env var or test key for development)
stripe.secret.key=${STRIPE_SECRET_KEY:sk_test_placeholder_for_local_dev}
